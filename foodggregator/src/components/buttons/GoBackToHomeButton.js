import React from 'react';

import { NavLink } from 'react-router-dom';

const GoBackToHomeButton = _ => {
  return (
    <NavLink to="/" className="GoBackToHomeButton">
      return home
    </NavLink>
  );
};

export default GoBackToHomeButton;
